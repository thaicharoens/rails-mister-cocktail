<h1><%= @cocktail.name %></h1>

<% @cocktail.doses.each do |dose| %>
  <li><%= dose.description %>
  || <%= dose.ingredient.name %> <%= link_to "Delete", dose_path(dose),method: :delete, data: { confirm: "Are you sure?" } %></li>
<% end %>

<%= link_to 'Add Ingredients', new_cocktail_dose_path(@cocktail.id) %>

<%= link_to 'Back to main', cocktails_path %>
